// Package protocol contains the (implementation first) JSON based protocol specification.
// This is the truth for both sides, backend (e.g. Go) and frontend (e.g. VueJS).
// TODO rename to ORA because it IS our UX specification and contract
package protocol
