package secret

// TODO we need per app and per user secrets, per user secrets may be e2e encrypted, but we need the key in the session to survive reboots and for async (cron) jobs, thus it does not make much sense!?
// TODO refactor smtp credentials here
