export enum RangeSelectionState {
	// if the start date should be selected next
	SELECT_START,
	// if the end date should be selected next
	SELECT_END,
	// if a start date and an end date has been selected
	COMPLETE,
}
